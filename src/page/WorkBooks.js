// WorkBooks.js
import React from 'react';


const WorkBooks = () => {

    return (
        <div>
            {/* {loginStatus ? (
                <h1>{`${userName}님, 환영합니다!`}</h1>
            ) : (
                <h1>로그인이 필요합니다.</h1>
            )}
            추가적인 컴포넌트 로직 */}
        </div>
    );
};

export default WorkBooks;
